# Build artifacts
.build/
dist/

# Compiled binaries / downloaded runtime
omatts
libonnxruntime.so*
*.dylib
*.dll
*.lib
*.exe

# ONNX models (too large for git — produced by export_onnx.py)
models/*
# but text rules and the generated pack config are versioned with the repo
!models/model_config.txt
!models/normalize.txt
models-de/*
!models-de/model_config.txt
!models-de/normalize.txt

# Voice samples are committed (small Opus files, ~600 KB total); only the
# embedding cache stays out.
voices/.cache/

# Python
.venv/
__pycache__/

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
