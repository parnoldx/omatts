# Build artifacts
.build/
dist/

# Compiled binaries / downloaded runtime
omatts
libonnxruntime.so*
*.dylib
*.dll
*.lib
*.exe

# ONNX models (too large for git — produced by export_onnx.py)
models/*
# but the text rules are versioned with the repo
!models/normalize.txt
models-german/
models-de/*
!models-de/normalize.txt
# German pack's shipped voices are committed (weights never are)
!models-de/voices-de/

# Voice samples are committed (small Opus files, ~600 KB total); only the
# embedding cache stays out.
voices/.cache/

# Python
.venv/
__pycache__/

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
